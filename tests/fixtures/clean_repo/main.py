"""A clean, well-written module."""


def add(first_number: int, second_number: int) -> int:
    """Add two numbers together."""
    return first_number + second_number


def greet(name: str) -> str:
    """Return a greeting message."""
    return f"Hello, {name}!"
